<?xml version="1.0" encoding="utf-8"?>
<manifest xmlns:android="http://schemas.android.com/apk/res/android"
    xmlns:tools="http://schemas.android.com/tools">

    <uses-permission android:name="android.permission.INTERNET" />
    <uses-permission android:name="android.permission.ACCESS_NETWORK_STATE" />
    <uses-permission android:name="android.permission.POST_NOTIFICATIONS" />
    <uses-permission android:name="android.permission.RECEIVE_BOOT_COMPLETED" />

    <application
        android:name=".VeterinariaApplication"
        android:allowBackup="true"
        android:dataExtractionRules="@xml/data_extraction_rules"
        android:fullBackupContent="@xml/backup_rules"
        android:icon="@mipmap/ic_launcher"
        android:roundIcon="@mipmap/ic_launcher_round"
        android:label="@string/app_name"
        android:supportsRtl="true"
        android:theme="@style/Theme.VeterinariaApp"
        tools:targetApi="31">

        <activity
            android:name=".LoginActivity"
            android:exported="true"
            android:theme="@style/Theme.VeterinariaApp">
            <intent-filter>
                <action android:name="android.intent.action.MAIN" />
                <category android:name="android.intent.category.LAUNCHER" />
            </intent-filter>
        </activity>

        <activity android:name=".MainActivity" android:exported="false" android:theme="@style/Theme.VeterinariaApp" />
        <activity android:name=".RegistroActivity" android:exported="false" android:theme="@style/Theme.VeterinariaApp" />
        <activity android:name=".ConsultaActivity" android:exported="false" android:theme="@style/Theme.VeterinariaApp" />
        <activity android:name=".ListadoActivityV4" android:exported="false" android:theme="@style/Theme.VeterinariaApp" />
        <activity android:name=".RecuperarContrasenaActivity" android:exported="false" android:theme="@style/Theme.VeterinariaApp" />

        <service android:name=".service.RecordatorioService" android:exported="false" />
    </application>
</manifest>